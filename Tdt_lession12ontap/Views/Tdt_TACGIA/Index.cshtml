@model IEnumerable<Tdt_lession12ontap.Models.Tdt_TACGIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tdt_TenTacGia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tdt_TenTacGia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Tdt_MaTG }) |
            @Html.ActionLink("Details", "Details", new { id=item.Tdt_MaTG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Tdt_MaTG })
        </td>
    </tr>
}

</table>
